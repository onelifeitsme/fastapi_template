## Setup Instructions

### 1. Configure `.env` File
In the `.env` file, specify the `DATABASE_URL` for your existing database.
For example:
DATABASE_URL="postgresql+asyncpg://postgres\:postgres@localhost:5432/YOUR_DB_NAME"

### 2.  Create folder versions in alembic folder

